<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pure black window background: on the X3 Pro waveguide, black is
         transparent — the world shows through and the projector rests. -->
    <style name="Theme.WanderQuest" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
